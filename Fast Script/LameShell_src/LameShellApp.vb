Module LameShellApp
    Public Sub Main()
        'make this XP style (make sure to set FlatStyle to system on all controls)
        System.Windows.Forms.Application.EnableVisualStyles()
        Application.DoEvents()
        Application.Run(New Form1)

    End Sub
End Module
